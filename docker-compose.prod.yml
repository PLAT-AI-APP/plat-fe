services:
  app:
    image: ${ECR_IMAGE}
    container_name: plat_fe_prod
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    networks:
      - app_network

networks:
  app_network:
    driver: bridge